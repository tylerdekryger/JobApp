DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/job_intelligence
TEST_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/job_intelligence_test
REDIS_URL=redis://localhost:6379/0
LOG_LEVEL=INFO
# Required for the fit/gap analysis on the search table + auto-discover. Leave blank to disable
# those features. If both keys are set, Gemini is preferred (free tier, no card required).
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
